// The OpenAPI version the API document declares. Its own module, with no imports, because the
// build script also reads it: a resume bullet names it, and the bullet must not drift from it.
export const OPENAPI_VERSION = '3.1.0';
