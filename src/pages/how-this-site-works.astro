---
import ArchitectureDiagram from '../components/ArchitectureDiagram.astro';
import Section from '../components/Section.astro';
import Base from '../layouts/Base.astro';
import { dataFile } from '../lib/data-file.ts';

// Structure and intro copy only. Every measured number and the what-broke list arrive once the
// site is live and measured, and are marked TODO below. The model in use is filled from
// /api/stats after load, never baked, so the page cannot go stale when the variable changes.
const dump = dataFile('portfolio.sql');
---

<Base title="How this site works" description="This site is a small full-stack system.">
  <main class="container">
    <header class="page-head">
      <h1>How this site works</h1>
      <p class="lead">This site is a small full-stack system. The content is Markdown and YAML. At build time it becomes a SQLite database and a set of JSON endpoints. In the browser, sql.js loads the database on demand so you can query it directly. One of the two server functions, /api/ask, sends your question and the schema to a language model, checks the SQL it returns by preparing it against the real database, and hands the SQL back to your browser to run. The model never touches the data. Below: the architecture, the validation rules, what it costs, and what broke.</p>
    </header>

    <Section id="architecture" title="Architecture">
      <ArchitectureDiagram />
      <!-- TODO: write a sentence introducing the curl one-liner: the plain-text resume for curl, wget and httpie. -->
      <pre><code>curl -A curl/8.0 https://alexkachur.com</code></pre>
    </Section>

    <Section id="build-time-database" title="Build-time database">
      <!-- TODO: describe the build step (scripts/build-db.ts, the seven tables, the determinism test) and add a sentence pointing at the readable dump linked below. -->
      <p><a href={dump.url}><code>/data/portfolio.sql</code></a></p>
    </Section>

    <Section id="the-ask-pipeline" title="The ask pipeline">
      <!-- TODO: describe the nine steps of the /api/ask pipeline in prose. -->
      <p>The model behind /api/ask is one environment variable, and Anthropic publishes each model's earliest retirement date, so a swap is a change of that variable and a re-recorded eval. <span hidden data-model-line>Right now it is <span data-model></span>.</span></p>
    </Section>

    <Section id="what-the-validator-rejects-and-why" title="What the validator rejects and why">
      <!-- TODO: describe the lexical pre-check and the engine validation ("the validator is the database"), with the validator's rules. -->
    </Section>

    <Section id="cost-and-the-cap" title="Cost and the cap">
      <!-- TODO: add the measured input and output token counts per question, the worst-case monthly ceiling at the default cap, the typical month, and why prompt caching is not used (the prompt is below Haiku 4.5's 4,096-token minimum and the volume would keep the cache cold). The numbers come from the live eval once /api/ask is running. -->
    </Section>

    <Section id="caching" title="Caching">
      <!-- TODO: describe the Redis response cache, its key scheme and TTLs, and that the footer is at most 60 seconds stale. -->
    </Section>

    <Section id="performance" title="Performance">
      <!-- TODO: list the JavaScript budget as the four files that load before interaction with their measured sizes, the Lighthouse mobile scores as text, and CLS. -->
    </Section>

    <Section id="what-broke" title="What broke">
      <!-- TODO: write this after the build. -->
    </Section>

    <Section id="what-i-left-out-on-purpose" title="What I left out on purpose">
      <!-- TODO: write a short list. -->
    </Section>
  </main>
</Base>

<style>
  .page-head {
    padding-top: var(--space-8);
  }

  @media (min-width: 768px) {
    .page-head {
      padding-top: var(--space-9);
    }
  }
</style>
